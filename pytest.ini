[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
markers =
    network: downloads a small public image (needs network once)
    val: runs model.val(); requires YOLO_VAL_DATA env pointing to dataset yaml
    slow: long-running tests
